[
  {
    "name": "DsButton",
    "extends": "ButtonHTMLAttributes<HTMLButtonElement>; className is appended to the base classes. This field is required: it documents which HTML attributes are forwarded and where className is applied. For components without a spread, use \"— no rest props\".",
    "description": "Template example component. Demonstrates the expected entry structure; the generator replaces this array completely with the target design system's inventory.",
    "props": [
      {
        "name": "variant",
        "type": "'primary' | 'secondary' | 'ghost'",
        "default": "'primary'",
        "required": false,
        "description": "Visual emphasis. Use at most one primary action per screen."
      },
      {
        "name": "size",
        "type": "'sm' | 'md'",
        "default": "'md'",
        "required": false,
        "description": "Height and internal spacing."
      },
      {
        "name": "disabled",
        "type": "boolean",
        "default": "false",
        "required": false,
        "description": "Standard button attribute."
      }
    ],
    "variants": {
      "variant": ["primary", "secondary", "ghost"],
      "size": ["sm", "md"]
    },
    "slots": [
      {
        "name": "children",
        "description": "Button label. Use an imperative verb with no punctuation."
      }
    ],
    "source": "src/components/DsButton.tsx"
  }
]
