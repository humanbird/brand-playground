# Changelog

All notable changes to brand-playground. Kits record the template commit they were generated from in `design/kit.json`.

## Unreleased

## 0.1.0 — 2026-09-01

First public release: the `/playground` generator skill, kit templates (React default, Vue), the kit contract (frozen tokens with provenance, `components-meta.json` as API source of truth, one distilled design-system skill + `craft.md`, `AGENTS.md`/`llms.txt` for any agent), auto-routing prototype loop, single-file export. Validated across live-site, code-first (React and Vue), Figma (sparse and rich) and spec inputs.
